---
description: Pre-flight check, then deploy evers.no to GitHub Pages
---

# Deploy evers.no

1. Run `git status` — show what's about to be committed
2. Run `git diff` — show me the changes
3. PAUSE: ask me "Commit and push?" with a suggested commit message based on the diff
4. If yes:
   - `git add .`
   - `git commit -m "<approved message>"`
   - `git push`
5. Confirm push succeeded
6. Note: live at evers.no in ~1 minute

Do NOT skip steps 1-3. Do NOT auto-commit without my approval.
